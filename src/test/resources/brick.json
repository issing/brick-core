{
    /** 认证模块 */
    auth : {
        test : {
            authorizer : {
                ignores : [ "test-domain:say", "test-domain:other", "say", "other" ]
            }
        }
    },
    /** 总线模块 */
    bus : {
        endpoints : {
            test : {
                class : "net.isger.brick.test.TestEndpoint"
            }
        }
    },
    /** 测试模块 */
    test : {
        test-domain : {
            something : "this is gate."
        }
    },
    /** 未知模块（系统忽略） */
    other : {
    }
}
